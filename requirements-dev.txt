# Dev-only dependencies: the gate `make check` runs. Install with
#   python -m pip install -r requirements-dev.txt
# Bump deliberately — CI installs exactly this file, so a new checker cannot
# change the gate on its own:
#   python -m pip install -U mypy && mypy && edit the pin here.
#   python -m pip install -U ruff && ruff check && edit the pin here.
mypy==2.3.1
ruff==0.16.7
# mypy's declared runtime deps — pin so CI cannot float past known-good.
ast-serialize==0.11.2
librt==0.15.0
mypy_extensions==1.1.0
pathspec==1.1.1
typing_extensions==4.16.0

# Optional runtime extras: pinned under [project.optional-dependencies]
# in pyproject.toml (numpy, rich + its tree, pillow). Install with:
#   python -m pip install '.[optional]'
