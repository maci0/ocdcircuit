# Testpoint: 4x4mm THT pad, drill 2.0 (single pin 1).
# NOTE: pad mirrors the hole (pads_of ignores hole-only footprints when the
# pads dict exists but is empty — bench-local workaround, not a code fix).
footprint TP1 4.0x4.0
pad 1 0 0 4.0 4.0
hole 1 0 0 2.0
body box 4.0 4.0 0.1
